{
  "name": "neo-mcp",
  "version": "1.1.30",
  "description": "Neo MCP server and local execution daemon — runs AI/ML tasks locally, no Python required",
  "main": "dist/index.js",
  "bin": {
    "neo-mcp": "bin/neo-mcp-daemon",
    "neo-mcp-daemon": "bin/neo-mcp-daemon"
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:dist": "node -e \"require('./dist/daemon.js')\"",
    "postinstall": "node scripts/postinstall.js",
    "prepublishOnly": "npm run build && npm test && npm run test:dist"
  },
  "engines": {
    "node": ">=18"
  },
  "files": [
    "dist",
    "bin",
    "scripts/postinstall.js",
    "skills",
    "README.md"
  ],
  "keywords": [
    "neo",
    "mcp",
    "daemon",
    "ai",
    "ml",
    "local-execution"
  ],
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0",
    "vitest": "^2.1.0"
  }
}
